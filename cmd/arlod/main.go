// Package main is the entrypoint for the Arlo daemon (arlod).
// arlod runs the AgentOS runtime: a gRPC server that manages agent lifecycles
// with an event-sourced persistence model.
package main

import "fmt"

func main() {
	fmt.Println("arlod: AgentOS daemon — not yet implemented")
}
